# Copy this file to `.env` and set your bot token. NEVER commit the real `.env`.
# This is the ONLY required setting: no server ID, no channel IDs, no role IDs.
#
# Token from: https://discord.com/developers/applications -> Bot -> Reset Token
DISCORD_TOKEN=paste_your_discord_bot_token_here
